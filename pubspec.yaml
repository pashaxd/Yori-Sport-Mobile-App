name: yori_sport_app
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.5.1

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  carousel_slider: ^5.0.0
  firebase_core: ^3.8.1
  firebase_auth: ^5.3.4
  firebase_ui_auth: ^1.16.0
  cloud_firestore: ^5.5.1
  provider: ^6.1.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  fonts:

    - family: Yori
      fonts:
        - asset: /Users/a1/StudioProjects/yori_sport_app/lib/assets/fonts/Anton-Regular.ttf
